# live-weather-grapher
Program that generates a 5-day weather forecast for a city using the OpenWeatherMap API.

To install the required modules please run
	pip3 install -r requirements.txt
	
Modules required
- requests
- matplotlib
